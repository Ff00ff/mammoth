{
  "name": "@ff00ff/mammoth",
  "license": "MIT",
  "version": "1.3.0",
  "main": "./.build/index.js",
  "types": "./.build/index.d.ts",
  "keywords": [
    "typescript",
    "postgres",
    "query",
    "builder",
    "sql",
    "typed",
    "database",
    "orm"
  ],
  "files": [
    ".build"
  ],
  "devDependencies": {
    "@tsd/typescript": "^5.2.2",
    "@types/jest": "^26.0.24",
    "@types/node": "^16.4.10",
    "jest": "^26.6.3",
    "jest-runner-tsd": "^6.0.0",
    "prettier": "^2.3.2",
    "ts-jest": "^26.5.6",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "test": "jest",
    "prebuild": "rm -rf .build && rm -rf coverage",
    "build": "tsc",
    "prepublishOnly": "npm run build && npm test"
  },
  "jest": {
    "projects": [
      {
        "displayName": "unit",
        "moduleFileExtensions": [
          "ts",
          "js"
        ],
        "coveragePathIgnorePatterns": [
          "tests"
        ],
        "testPathIgnorePatterns": [
          "/\\.build/",
          "/node_modules/",
          "helpers"
        ],
        "testMatch": ["**/tests/unit/*.test.ts"],
        "transform": {
          "\\.ts$": "ts-jest"
        },
        "reporters": [
          "default"
        ]
      },
      {
        "displayName": {
            "color": "blue",
            "name": "types"
        },
        "runner": "jest-runner-tsd",
        "testMatch": ["**/tests/types/*.test.ts"]
      }
    ]
  }
}
